"use strict";
const assert = require('assert');
const debug = require('../../../server/socketIORoutes/inbound/debug.js');
describe('Server/socketIORoute/inbound/debug.js', function () {
	before(function() {
		// runs before all tests in this block
	});
	it('should exist', function() {
		assert.ok(debug !== undefined)
	});

});